<Window x:Class="ControllerLocal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Label Grid.Column="0" Content="Width" />
        <TextBox Grid.Column="1" HorizontalAlignment="Stretch" Text="20" 
                 VerticalAlignment="Center" Width="80" TextAlignment="Right" />

        <Label Grid.Column="2" Content="Height" />
        <TextBox Grid.Column="3" HorizontalAlignment="Stretch" Text="20" 
                 VerticalAlignment="Center" Width="80" TextAlignment="Right"/>

        

    </Grid>
</Window>
